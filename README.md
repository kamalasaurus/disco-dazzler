# disco-dazzler

## usage

```
  // should assign whatever class you want to sparkle to the elements
  // you want to sparkle -- pretty agnostic api
  DiscoDazzler(document.querySelectorAll('.sparkle-true'), {}, true);
  DiscoDazzler(document.querySelectorAll('.sparkle'), {color: 'rainbow'});
```

